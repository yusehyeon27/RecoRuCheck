{
  "name": "recoru-checker",
  "version": "1.0.0",
  "bin": "main.js",
  "scripts": {
    "build": "pkg . --targets node18-win-x64 --output recoru-checker.exe"
  },
  "dependencies": {
    "playwright": "^1.47.0",
    "prompt-sync": "^4.2.0",
    "nodemailer": "^6.9.0",
    "dotenv": "^16.0.0"
  }
}
